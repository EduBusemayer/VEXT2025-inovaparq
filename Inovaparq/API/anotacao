'''

pip install uvicorn
python -m uvicorn API.main:app --reload


CREATE DATABASE IF NOT EXISTS inovaparq CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE inovaparq;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(100) NOT NULL,
    profile VARCHAR(20) NOT NULL
);

CREATE TABLE startups (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description VARCHAR(255) NOT NULL,
    incubator VARCHAR(50) NOT NULL,
    stage VARCHAR(50) NOT NULL
);

Caso o local host ficar travado 
netstat -ano | findstr :8000
taskkill /PID 1234 /F

{
  "name": "João Silva",
  "email": "joao.silva@email.com",
  "password": "senhaSegura123",
  "profile": "startup"
}

{
  "name": "InovaTech",
  "description": "Soluções inovadoras para o mercado de tecnologia.",
  "incubator": "CENTRA",
  "stage": "Implantação"
}


'''